# 小说→SSML 流水线 环境变量
# 复制为 .env 并填写。LLM 配置可从墨语主项目的 .env 里复用同一组。

# LLM(OpenAI 兼容接口)
# 支持智谱GLM / DeepSeek / Moonshot / OpenAI / 自建中转等
LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
LLM_API_KEY=your_api_key_here
LLM_MODEL=glm-4-plus

# 服务端口(默认 8001,避免和墨语后端冲突)
PORT=8001
