# Ruff 代码检查报告解读（744 个问题分类）

> 生成时间：2026-06-27
> 检查工具：ruff 0.15.20 + 配置见 `backend/pyproject.toml`

## 严重程度图例

| 标记 | 含义 | 建议 |
|------|------|------|
| 🟢 | **格式/风格**，能一键自动修，零风险 | 直接用 `ruff check --fix` 修 |
| 🔵 | **写法可改进**，自动修安全，建议修 | 直接自动修 |
| 🟡 | **需人工判断**，自动修可能改变行为 | 逐个看 |
| 🔴 | **真 bug 或高危**，必须人工修 | 重点处理 |

---

## 🟢 格式/风格问题（可一键自动修，零风险）

| 规则 | 数量 | 大白话解释 | 例子 |
|------|------|-----------|------|
| **W293** | 134 | 空白行里有多余的空格 | `···\n`（三个点代表空格）→ 删掉空格 |
| **I001** | 115 | import 导入顺序乱了，没按规则排 | `import` 该按「标准库→第三方→本项目」分组并排序 |
| **W292** | 33 | 文件最后一行没有换行符 | 文件结尾要有一个空行（POSIX 规范） |
| **F541** | 26 | 写了 `f"..."` 但里面没有 `{变量}` | `f"hello"` 里没变量 → 去掉前缀 `f` |
| **W291** | 1 | 行尾有多余空格 | `"foo "  ` → `"foo"` |
| **E401** | 1 | 一行写了多个 import | `import os, sys` → 分两行 |

**小计：310 个，全部可安全自动修。**

---

## 🔵 写法可现代化（自动修安全，建议修）

| 规则 | 数量 | 大白话解释 | 例子 |
|------|------|-----------|------|
| **UP045** | 116 | `Optional[X]` 是旧写法，新写法用 `X \| None` | `Optional[str]` → `str \| None` |
| **UP006** | 7 | `typing.List` 是旧写法，直接用内置 `list` | `List[str]` → `list[str]` |
| **UP007** | 1 | `Union[X, Y]` 旧写法，用 `X \| Y` | `Union[str, int]` → `str \| int` |
| **UP015** | 4 | `open(f, "r")` 的 `"r"` 是多余的（默认就是读） | `open(f, "r")` → `open(f)` |
| **UP032** | 1 | `"{x}".format(x=1)` 可改成 f-string | → `f"{1}"` |
| **UP041** | 1 | `socket.timeout` 旧别名，应用 `TimeoutError` | |

**小计：130 个，全部可安全自动修。**（Python 3.12 支持新语法，项目已用 3.12）

---

## 🟡 需要人工判断的问题（自动修可能改变行为，建议逐个看）

| 规则 | 数量 | 大白话解释 | 为什么要人工看 |
|------|------|-----------|---------------|
| **E701** | 122 | 一行写了带冒号的复合语句 | `if x: do()` 这种单行写法，改多行可能影响可读性偏好 |
| **E702** | 16 | 一行用分号写了多条语句 | `a=1; b=2` 一般建议拆行，但偶尔是刻意的 |
| **E402** | 18 | import 不在文件顶部 | 有些是有意为之（如延迟导入避免循环依赖），不能动 |
| **E741** | 18 | 变量名用了 `l` `I` `O` 这种易混淆的名字 | 改名可能影响其他代码，要确认 |
| **N806** | 6 | 函数里用了大写变量名（按规范小写） | 可能是常量，要看上下文 |
| **N803/N814** | 2 | 参数名/导入命名不规范 | 改名要谨慎 |
| **SIM103** | 5 | `if cond: return True else: return False` 可简化 | → `return cond`，行为等价但要确认 |
| **SIM105** | 5 | `try/except: pass` 可用 `contextlib.suppress` | 纯写法偏好 |
| **SIM108** | 1 | if/else 可改成三元表达式 | 可读性偏好 |
| **SIM113** | 1 | 手动计数循环可改用 `enumerate` | 纯优化 |
| **C401/C408/C416** | 4 | 不必要的列表推导/构造 | 小优化 |
| **B007** | 1 | 循环变量没用 | 可能是占位 |
| **B905** | 1 | `zip()` 没指定 `strict` | Python 3.10+ 新参数，加上更安全 |
| **F811** | 1 | 重复定义（覆盖了之前的） | 要确认是有意覆盖还是笔误 |

**小计：约 200 个，需逐个看。**

---

## 🔴 真 bug 或高危（必须人工修复！）

| 规则 | 数量 | 大白话解释 | 严重性 |
|------|------|-----------|--------|
| **F821** | 2 | **用了未定义的变量**——代码跑到这里会直接报 `NameError` 崩溃！ | ⚠️ 真 bug |
| **F401** | 63 | import 了但从来没用（死导入） | 清理即可，但删之前确认没被 `import *` 间接用 |
| **F841** | 12 | 变量赋值了但从来没读取 | 多半是忘删的调试代码 |
| **UP035** | 9 | 用了已废弃的导入（如 `typing.Dict`） | 换成 `dict` 即可，但要确认 |
| **B904** | 8 | `except` 里 `raise` 没加 `from`，会丢失原始错误链 | 调试时难定位错误根因 |
| **E722** | 5 | 裸 `except:`（不写异常类型），会吞掉所有错误包括 `KeyboardInterrupt` | 危险，建议改成 `except Exception:` |
| **B006** | 2 | 函数默认参数用了可变对象（如 `def f(x=[])`)——**经典 Python 坑**！默认值会在调用间共享 | 容易导致诡异 bug |
| **B023** | 2 | 循环里的闭包/lambda 捕获了循环变量——会取到**最后一次**的值 | 经典闭包陷阱 |

**小计：约 100 个，其中 F821 的 2 个是真崩溃 bug，优先级最高。**

---

## 已知的 2 个真 bug（F821，建议优先修）

1. **`app/api/routes/project_init.py:377`**
   - 代码用了变量 `occ_raw`，但它从来没定义过
   - 后果：项目初始化时执行到角色职业匹配会崩溃
   - 应该是漏写了一行从 `item` 里取职业的逻辑

2. **`app/services/chapter_context_service.py:866`**
   - 类方法里用了 `db.execute`，但应该是 `self.db.execute`（类里其他地方都用 `self.db`）
   - 后果：章节上下文构建时写作风格加载会崩溃

---

## 怎么修？（按风险从低到高）

### 第 1 步：一键自动修 485 个安全项（零风险）
```bash
cd backend
venv/bin/ruff check . --fix          # 修 🟢🔵 类
venv/bin/ruff format .               # 统一格式
git diff --stat                       # 看改了哪些文件
```
> 这会修掉：W293/W292/W291/F541/I001/E401（格式）+ UP045/UP006/UP007/UP015/UP032/UP041（现代化）+ F401/F811 部分。

### 第 2 步：人工修 2 个真 bug（F821）
> 见上文「已知的 2 个真 bug」，建议立刻修。

### 第 3 步：人工审查 🔴 高危项（B904/E722/B006/B023）
> 这些是真实的代码质量问题，逐个看。

### 第 4 步：按需处理 🟡 风格项
> 200 个左右，不急，有空慢慢改。

---

## 懒人版建议

如果你只想**先让它通过检查**（不纠结代码质量），可以在 `backend/pyproject.toml` 的 `ignore` 里加上：
```toml
ignore = [
    "E501", "B008", "E712", "SIM114",
    # 暂时忽略需人工判断的（让 CI 先绿）
    "E701", "E702", "E402", "E741", "N806",
]
```
但这只是「不报」，问题还在。建议至少把 F821（真 bug）和 B006（经典坑）修掉。